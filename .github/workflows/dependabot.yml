name: Dependabot

on: pull_request_target

permissions:
  pull-requests: write
  contents: write

jobs:
  dependabot:
    name: Pull Request Approve and Merge
    uses: osinfra-io/github-misc-called-workflows/.github/workflows/dependabot.yml@main
